#include "basicalgorithm.h"

BasicAlgorithm::BasicAlgorithm(int _window_size, int _radius, int _peak_threshold) {
    window_size = _window_size;
    radius = _radius;
    peak_threshold = _peak_threshold;
}

std::vector<Fixation>& BasicAlgorithm::generateFixations() {

}
